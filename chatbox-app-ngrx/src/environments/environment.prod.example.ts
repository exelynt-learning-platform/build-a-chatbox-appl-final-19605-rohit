/**
 * Production environment template — COPY this file to environment.prod.ts and fill in your key.
 * NEVER commit environment.prod.ts to version control.
 */
export const environment = {
  production: true,
  openAiApiKey: 'YOUR_OPENAI_API_KEY_HERE',
  openAiModel: 'gpt-3.5-turbo'
};
